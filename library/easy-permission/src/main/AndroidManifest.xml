<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <activity
            android:name="top.amake.permission.PermissionActivity"
            android:theme="@style/ThemeTranslucent" />
        <provider
            android:name="top.amake.permission.PermissionProvider"
            android:authorities="${applicationId}:PermissionProvider"
            android:exported="false" />
    </application>
</manifest>